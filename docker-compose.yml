services:
  qr-scanner-dev:
    build: .
    container_name: qr-scanner-dev
    ports:
      - "5000:5000"
    restart: unless-stopped
    volumes:
      # 🔥 DESARROLLO: Monta el código para cambios en vivo
      - ./app.py:/app/app.py
      - ./requirements.txt:/app/requirements.txt
      # 📋 Logs persistentes
      - ./logs:/app/logs
    environment:
      - PORT=5000
      - FLASK_ENV=development
    # 🔄 Reinicio automático en cambios de código
    command: python -u app.py